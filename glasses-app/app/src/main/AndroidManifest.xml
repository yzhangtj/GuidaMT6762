<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for camera, microphone, and network access -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
                     android:maxSdkVersion="28" />
    
    <!-- Additional permissions for radar sensor -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    
    <!-- WiFi permissions for Android 10+ -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- Camera features -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    
    <!-- Sensor permissions -->
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- Bluetooth permissions for Android 12+ and legacy support -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Package visibility queries for Android 11+ -->
    <queries>
        <!-- Query for Google TTS package -->
        <package android:name="com.google.android.tts" />
        
        <!-- Query for any TTS service -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuidaApp0606"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GuidaApp0606"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:keepScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:theme="@style/Theme.GuidaApp0606"
            android:parentActivityName=".MainActivity" />
        
        <activity
            android:name=".RadarViewActivity"
            android:label="Radar View"
            android:theme="@style/Theme.GuidaApp0606"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" />
        
        <activity
            android:name=".LogViewActivity"
            android:label="Target Logs"
            android:theme="@style/Theme.GuidaApp0606"
            android:parentActivityName=".RadarViewActivity"
            android:screenOrientation="portrait" />
        
        <activity
            android:name=".SensorCliProbeActivity"
            android:label="Sensor Probe"
            android:theme="@style/Theme.GuidaApp0606"
            android:screenOrientation="portrait"
            android:exported="true" />

        <activity
            android:name=".RadarDebugActivity"
            android:label="Radar Debug"
            android:theme="@style/Theme.GuidaApp0606"
            android:screenOrientation="portrait"
            android:exported="true" />
    </application>

</manifest>